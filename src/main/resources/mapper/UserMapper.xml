<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="voiceCare.mapper.UserMapper">

    <!--注册，insert；values取User对象的属性-->
    <insert id="save" parameterType="User">

        INSERT  INTO user (name, pwd, record_aud, tone_id, head_img, phone , create_time, family_id)
        values (#{name,jdbcType=VARCHAR}, #{pwd,jdbcType=VARCHAR}, '0', 0, #{headImg,jdbcType=VARCHAR},
        #{phone,jdbcType=VARCHAR},#{createTime,jdbcType=TIMESTAMP},#{familyId,jdbcType=VARCHAR})

    </insert>


    <!--根据手机号和密码找用户-->
    <select id="findByPhoneAndPwd" resultType="User">

        select  * from user where phone =#{phone} and pwd = #{pwd}

    </select>

    <select id="findByUserId" resultType="User">

      select  * from user where id=#{user_id}

    </select>

    <select id="findListFamily" resultType="User">

        select * from user where id != #{user_id} AND family_id=#{family_id} order by create_time desc

    </select>

    <update id="createFamily" parameterType="User">
        update user set family_id=#{familyId} where phone=#{phone}
    </update>

<!--    <select id="findAudioUrlByUserId" resultType="String">-->

<!--        select audio_url from user where id=#{user_id}-->

<!--    </select>-->
</mapper>